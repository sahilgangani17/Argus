# Argus authorization scope file
# Only hostnames listed here may be scanned. Anything else is rejected
# at the request layer before a single packet goes out.

authorized_targets:
  - 127.0.0.1
  - localhost
  - dvwa.local

owner: "Hackathon Demo — Team Argus"
signed: true
notes: >
  Demo scope for a local DVWA instance (docker run -d -p 8080:80 vulnerables/web-dvwa).
  Do not add live/public targets without explicit written authorization.
